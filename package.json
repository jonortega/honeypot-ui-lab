{
  "name": "honeypot-ui-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "pnpm@10.10.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "build:api": "pnpm --filter api build",
    "start:api": "pnpm --filter api start"
  },
  "devDependencies": {
    "@types/node": "^22.9.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
